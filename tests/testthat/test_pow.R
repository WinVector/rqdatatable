library("rqdatatable")

context("pow")

test_that("test_pw: Works As Expected", {
  ## TODO: wait on rquery 1.2.1 for this test
  # d <- data.frame(x = 2)
  # d2 <- extend(d, xsq = x^2)
  # testthat::expect_equivalent(data.frame(x = 2, xsq = 4), data.frame(d2))
})
